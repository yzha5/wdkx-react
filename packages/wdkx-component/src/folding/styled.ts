import styled from '@emotion/styled'

export const StyledFolding = styled.div({
    label: 'folding',
    width: '100%',
})

export const StyledFoldingResident = styled.div({
    label: 'folding-resident',
})

export const StyledFoldingPanel = styled.div({
    label: 'folding-panel',
})
